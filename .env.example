# The default production pipeline is static and has no server-side credentials.
# Keep this file free of API keys and database credentials.

# Optional public verification meta tag used by Astro at build time.
PUBLIC_GOOGLE_SITE_VERIFICATION=
